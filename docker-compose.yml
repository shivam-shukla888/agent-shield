# ============================================================
# AgentShield — Docker Compose Production Stack
# ============================================================
# Usage:
#   cp .env.example .env   # fill in real values
#   docker compose up -d
#   docker compose logs -f agentshield
# ============================================================

services:
  # ---------- AgentShield Application ----------
  agentshield:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: agentshield-app
    restart: unless-stopped
    ports:
      - "${APP_PORT:-8000}:8000"
    environment:
      - APP_HOST=${APP_HOST:-0.0.0.0}
      - APP_PORT=8000
      - LOG_LEVEL=${LOG_LEVEL:-INFO}
      - AGENTSHIELD_API_KEY=${AGENTSHIELD_API_KEY:-}
      - AGENTSHIELD_RATE_LIMIT_RPM=${AGENTSHIELD_RATE_LIMIT_RPM:-60}
      - DATABASE_URL=postgresql://agentshield:${POSTGRES_PASSWORD:-agentshield}@postgres:5432/agentshield
      - AGENTSHIELD_LLM_PROVIDER=${AGENTSHIELD_LLM_PROVIDER:-fake}
      - AGENTSHIELD_LLM_API_KEY=${AGENTSHIELD_LLM_API_KEY:-}
      - AGENTSHIELD_LLM_MODEL=${AGENTSHIELD_LLM_MODEL:-}
      - AGENTSHIELD_LLM_TIMEOUT=${AGENTSHIELD_LLM_TIMEOUT:-30.0}
      - AGENTSHIELD_LLM_ENDPOINT=${AGENTSHIELD_LLM_ENDPOINT:-}
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health/ready"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s
    networks:
      - agentshield-net

  # ---------- PostgreSQL Database ----------
  postgres:
    image: postgres:16-alpine
    container_name: agentshield-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: agentshield
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-agentshield}
      POSTGRES_DB: agentshield
    volumes:
      - agentshield-pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U agentshield -d agentshield"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    networks:
      - agentshield-net

# ---------- Persistent Volumes ----------
volumes:
  agentshield-pgdata:
    driver: local

# ---------- Isolated Network ----------
networks:
  agentshield-net:
    driver: bridge
